// Minimal ambient module declaration to satisfy TypeScript
// during Next.js build when resolving the `pg` package.
// The actual runtime types are provided by the `pg` package itself.

declare module "pg";

